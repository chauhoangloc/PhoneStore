<ul class="dropdown-menu">
    @foreach (var item in ViewBag.TenLoaiSP)
    {
        <li>
            <a href="@Url.Action("Index","shopping",new { MaLoaiSP= item.Key})">
                @item.Value
            </a>
        </li>
    }
</ul>

